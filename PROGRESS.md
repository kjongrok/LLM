# AI_1team 프로젝트 진행 상황 (Phase 1 & Phase 2)

## 🎯 프로젝트 전체 목표
- **[Phase 1] Deep Learning 관제 (완료):** CCTV 기반 교통량/교통 상황 예측 및 분석 (YOLOv8 + GRU + LSTM Autoencoder)
- **[Phase 2] LLM 챗봇 연동 (진행 예정):** 실시간 교통 데이터를 분석하고 자연어로 사용자에게 상황을 보고/예측해 주는 지능형 AI 에이전트 구축

---

## 🏆 [Phase 1: Deep Learning] 딥러닝 비전 및 예측 파이프라인 최종 완성 내역 (Finalized)

본 섹션은 심사위원 방어 및 기술 시연(Demo)을 위해 최종적으로 확정된 핵심 기술 스펙과 트러블슈팅 내역을 요약합니다.

### 1. 웹 관제 대시보드 (Streamlit) 구축 완료
- **초고도화 UI/UX:** `app.py` 단일 파일로 실행되는 통합 웹 대시보드 구축.
- **핵심 3-Tab 체제 확립:** 
  1. `실시간 관제 (Live)`: CCTV 스트리밍 및 비전 분석.
  2. `모델 방어 보고서`: GRU 챔피언 모델 선정 논리(R² Score 0.871) 등 심사 방어 방패 마련.
  3. `아키텍처 & MLOps 파이프라인`: 시스템 다이어그램 및 Supabase 실시간 적재 데이터 EDA 테이블 병합.
- **2-Depth CCTV 필터링:** 고속도로(EX)와 국도/지방도(ITS) CCTV를 직관적으로 분리 선택할 수 있는 사이드바 UI 최적화.

### 2. 비전(Vision) 엔지니어링: 추적(Tracking) 및 필터링 고도화
- **독자적 센트로이드 트래커(Custom Centroid Tracker) 도입:** 
  - 기본 제공되는 ByteTrack이 프레임 스킵 환경에서 객체를 놓치는 치명적 버그(Frame Drop Issue)를 발견하고 전면 폐기.
  - 유클리디안 거리 연산 기반의 독자적인 2D 트래킹 알고리즘을 직접 코딩하여 인식률 개선.
- **정차 차량(Parked Car) 필터링:** 
  - 갓길 주차나 톨게이트 정차 차량 때문에 혼잡도가 폭증하는 오탐지(False Alarm)를 원천 차단.
  - 트래커가 10프레임 이상 1픽셀 미만으로 움직인 객체를 `[Parked]` 회색 박스로 분류하고, 혼잡도 연산에서 정상적으로 제외 처리.

### 3. 시계열 예측(Forecasting): GRU 딥러닝 앙상블 아키텍처
- **가짜 추세선(Linear Polyfit) 전면 폐기:** 점선이 튀는 것을 막기 위해 임시로 썼던 수학적 추세선을 삭제.
- **정통 Auto-Regressive GRU 복원:** 실제 훈련된 `forecaster_model.keras` (GRU) 모델을 복원하여 `t+1`부터 `t+5`까지 꼬리를 무는 다중 스텝 추론 구현.
- **[6:4 앙상블 기법] 극한의 안정성 확보:** 데이터 노이즈로 인한 예측 차트 널뛰기(Flickering)를 잡기 위해, `[딥러닝(GRU) 예측값 60%] + [과거 12프레임 시계열 트렌드 40%]`를 섞어내는 하이브리드 수식(Ensemble) 발명 및 적용 완료.

### 4. 돌발 정체 주입 (Stress Test) 시뮬레이션
- 심사위원 시연 시, 교통량이 한산한 도로에서도 강제로 차량 대수를 35대 추가하고 밀집도를 폭증시키는 `[🚨 돌발 정체 주입]` 토글 버튼 완성.
- 이를 통해 예측 모델(GRU)과 이상 탐지 모델(LSTM Autoencoder)이 어떻게 반응하고 경보(ANOMALY) 울리는지 명확한 시각적 검증 가능.

---

## 🚀 [Phase 2: LLM] 다음 세션에서 진행할 작업 (To-Do)

본격적으로 거대 언어 모델(LLM)을 프로젝트에 연동하여, 숫자로만 표시되던 관제 시스템에 '지능(Intelligence)'을 부여합니다.

## 🎯 4단계: 실시간 딥러닝 비전 + LLM 융합 (현재 진행 중)
본격적으로 거대 언어 모델(LLM)을 프로젝트에 연동하여, 숫자로만 표시되던 관제 시스템에 '지능(Intelligence)'을 부여합니다.

- `[x]` **LLM 기술 스택 선정:** Groq API(Llama 3)를 백엔드로 사용 결정 및 연동 완료.
- `[x]` **System Prompt (페르소나) 설계:** 
  - LLM에게 "ITS 관제 요원" 역할 부여 및 Streamlit 4번 탭에 채팅 UI 구축 완료.
- `[x]` **데이터 실시간 주입 (RAG/Context Injection):** 
  - 스트리밍 종료 직전의 실시간 데이터(live_metrics) 및 혼잡도 추이를 LLM에 주입 성공.
  - 대화 기록(Context Memory) 연동 로직 정상화 완료.
- `[x]` **대국민/관리자용 리포팅 자동화:** 
  - "현재 수원 신갈 IC 교통 상황 요약해 줘", "5분 뒤 정체될까?" 같은 질문에 LLM이 대답하는 로직 퀄리티 고도화 완료.
  - **[신규 킬러 기능]** 지정된 시간(1시간 등)의 DB 로그를 분석해 결재용 '관제 일지'를 자동 작성하는 RPA 기능(Auto Report Generator) 구현 완료.
- `[x]` **심사위원 어필 포인트 기획:** 단순한 챗봇이 아닌, 실시간 딥러닝 비전 데이터와 LLM이 상호작용하고 날씨나 과거 DB까지 융합하는 **"Vision-to-Text MLOps"** 구조임을 강조할 논리 및 실제 RAG 기능 구현 완료.

---

## 🛠️ 최근 트러블슈팅 및 아키텍처 결정 이력 (History)

**1. ITS 공공데이터 API 연결 복구**
- 기존 `urllib` 기반 코드에서 `HTTPSConnectionPool` 에러 발생.
- `requests` 라이브러리로 전면 교체 후 JSON 파싱 로직 최적화하여 정상 작동 확인.

**2. LLM 대화 기억 상실(Amnesia) 버그 해결**
- **이슈:** 챗봇이 직전에 나눈 대화를 기억하지 못하고 매번 초기화됨.
- **원인:** Groq API에 새 질문 1개만 단독으로 전달하고 있었음.
- **해결:** `st.session_state.messages` 전체를 API에 전달. 단, **Llama-3 모델은 대화가 반드시 User 역할로 시작해야 하는 제약(400 Bad Request)**이 있어, 챗봇의 첫 초기 환영 인사(assistant)는 Context에서 제외하는 예외 처리 로직 추가 완료.

**3. Streamlit 동시성(UI 블로킹) 한계 극복 시도 및 롤백**
- **목표:** CCTV 영상(무한 루프)이 돌아가는 중에도 사용자가 4번 탭에서 LLM 채팅을 칠 수 있도록 만들기 위해 시도.
- **과정:** 영상 처리를 별도의 백그라운드 스레드(`video_worker`)로 빼고, 프론트엔드는 `st_autorefresh`로 0.5초마다 갱신하는 멀티스레딩 아키텍처 도입.
- **이슈:** 
  - 파이썬 GIL(Global Interpreter Lock) 제약과 데모 영상(MP4)의 빠른 재생 속도가 충돌하여 CPU 100% 점유 현상 및 전체 화면 프리징(멈춤) 발생.
  - 차트(Vega-Lite)를 초당 여러 번 억지로 렌더링하면서 극심한 프론트엔드 렉 발생 및 `FutureWarning` 경고 발생.
- **결정 (Rollback):** 무리한 멀티스레딩이 오히려 시연 안정성을 크게 해치므로, **가장 안정적인 단일 스레드(Single-Thread) 원본 코드로 전면 롤백 결정.** (`frame_skip = 10` 원복)
- **최종 시연 시나리오 확정:** 영상 스트리밍을 켜서 분석 결과를 충분히 확보한 뒤, **스트리밍 체크박스를 끄고 4번 탭으로 넘어가서 분석된 최신 데이터(`live_metrics`)를 바탕으로 LLM과 대화하는 안정적인 시퀀스**로 채택.

**4. LLM 완전한 '문맥' 및 '데이터' 기억 상실 버그 해결**
- **이슈:** 챗봇이 이전 대화 내용을 하나도 기억 못하고, 실시간 CCTV 데이터조차 '알 수 없음'으로 대답함.
- **해결 1 (문맥 상실):** 사용자의 질문만 `st.session_state.messages`에 저장하고 AI의 답변은 저장하지 않는 치명적 버그 발견. AI 답변 직후 기록을 append 하도록 로직 수정 및 중복 append 버그 동시 해결.
- **해결 2 (데이터 상실):** 이전 원본 코드로 롤백하는 과정에서 스트리밍 루프(`while run_stream:`) 내부에 있던 `live_metrics` 업데이트 코드가 날아간 것을 확인하여 다시 복구.

**5. Supabase 연동 실시간 과거 데이터 RAG 파이프라인 구축**
- **목표:** "최근 1분간의 평균 통행량은 어때?" 와 같은 과거 데이터 질의에 챗봇이 정확히 대답하도록 개선.
- **해결:** LLM 호출 직전에 Supabase DB에서 최근 60개(1분 분량)의 로그를 긁어와 승용차, 버스, 트럭의 평균 통행량과 혼잡도를 계산한 뒤 LLM의 System Prompt에 `[과거 누적 통계 데이터]`라는 명찰을 달아 강제 주입함. 프롬프트 수칙을 엄격하게 분리하여 '현재'와 '과거'를 혼동하지 않도록 튜닝 완료.

**6. 채팅창 UI 프레임 분리 (UX 개선)**
- **이슈:** 대화가 길어질수록 웹페이지가 무한정 아래로 길어지는 촌스러운 UI.
- **해결:** Streamlit 1.35+ 의 `st.container(height=600)` 기능을 사용하여 카카오톡이나 ChatGPT처럼 내부에 스크롤이 생기는 독립된 고정 크기 채팅창(프레임)으로 UI 퀄리티 대폭 업그레이드.

**7. 킬러 기능 추가: 관제 일지 원클릭 자동 생성기 (RPA)**
- **목표:** 단순한 챗봇 응답을 넘어선 실무 수준의 관리자용 리포팅 업무 자동화 구현.
- **해결:** 채팅창 상단에 전용 Expander를 만들어, '최근 10분, 30분, 1시간'의 방대한 DB 데이터를 한 번에 가져와 LLM에게 전문적인 마크다운 형식의 결재용 '지능형 ITS 교통 관제 일지'를 작성하도록 지시하는 버튼과 로직 구현 완료. (심사위원 어필용 핵심 포인트 확보)

**8. LLM 자율 DB 조회 (Function Calling) 도입 및 UI 분리**
- **목표:** 기존에 고정된 1분(60개) 분량의 과거 데이터만 주입하던 한계를 극복하고, 사용자의 질문에 따라 필요한 시간대의 데이터를 유연하게 조회.
- **해결:** Llama-3 모델의 `tool_calls` 기능을 활용하여 `get_traffic_data_from_db` 함수를 도구로 제공. LLM이 스스로 과거 조회가 필요하다고 판단하면 해당 도구를 호출하여 지정된 분(minutes) 만큼 Supabase에서 가져와 대답하도록 개선(Agentic Workflow 적용).
- **UI 개선:** 기존 4번 탭에 있던 '관제 일지 자동 생성기' 기능을 별도의 **5번 탭**으로 독립적으로 분리하여 챗봇 UI의 쾌적함 확보.

**9. LLM DB 연동(Function Calling) 치명적 버그 해결 및 정상화**
- **이슈:** Llama 3.1 8B, Mixtral 모델 등에서 DB 조회 시 `tool_use_failed` 또는 `model_decommissioned` 환각/에러가 지속적으로 발생하여 정확한 과거 조회가 불가능.
- **해결:** 에러를 뱉는 폐기된 모델 및 불안정한 경량 모델들을 UI 옵션에서 모두 제거. 모델이 시간/날짜 범위를 올바르게 인식하도록 백엔드에 정교한 정규식 기반 시간 파서(Date Parser)를 구축. 충분한 토큰이 확보된 `llama-3.3-70b-versatile` 모델을 기본 탑재하여, "어제 교통량", "최근 1시간", "7월 1일" 등의 쿼리를 정밀한 크로스체크(실제 DB 수치와 정확히 일치) 수준으로 응답하도록 환각(Hallucination) 버그 제어.
- **추가:** 클라우드 LLM 토큰 고갈 및 장애 상황을 대비하여, Local LLM(Ollama) 베이스 URL 인젝션 기능을 추가하여 오프라인/무제한 환경에서도 시스템이 구동될 수 있는 안전장치 마련.

**10. 관제 일지(보고서) Word 파일(.docx) 자동 추출 기능 신설**
- **목표:** 기존 5번 탭의 단순 텍스트 출력 방식에서 한 단계 나아가, 실무진들이 2차 가공 및 실제 결재에 즉각 활용할 수 있는 문서 포맷 제공.
- **해결:** `python-docx` 라이브러리를 도입. LLM이 생성한 마크다운 문법(Heading, Bullet points 등)을 파이썬 백엔드에서 실시간으로 인메모리 파싱하여 굵은 글씨, 폰트 사이즈가 적용된 정식 회사 결재 양식 형태의 `.docx` 파일로 조립한 후 사용자에게 다운로드 버튼 제공.

**11. UI/UX 전면 프리미엄화 (다크 모드 & 글래스모피즘)**
- **목표:** Streamlit의 기본 화이트보드 템플릿을 탈피하여, 공상과학 수준의 미래지향적/프리미엄 관제 대시보드 시각화.
- **해결:** `st.markdown` Custom CSS 인젝션을 활용. 글로벌 폰트를 가독성이 뛰어난 'Pretendard'로 강제 적용하고, 메인 배경을 네이비 블루 그라데이션으로 전면 교체. 지표(Metric) 카드와 탭 컴포넌트에 반투명 유리 질감(Glassmorphism) 및 마우스 오버 시 애니메이션(Hover Transition)을 주입하여 세련된 상용 어플리케이션 퀄리티 확보.

**12. 딥러닝 비주얼 고도화 (실시간 혼잡도 히트맵 및 가상 통제선 카운팅)**
- **목표:** 백엔드에서만 돌아가는 AI 모델의 위력을 심사위원들이 화면상에서 직관적으로 체감할 수 있도록 시각적 임팩트(Visual Impact) 부여.
- **해결:** OpenCV의 가우시안 필터링과 Jet 컬러맵을 조합하여 차량 밀집 구간을 붉게 물들이는 '실시간 히트맵(Heatmap)' 알고리즘을 구축. 더불어, 객체의 고유 ID를 기억하는 트래킹 기술을 응용하여 화면 특정 위치(y=0.6)에 가상의 톨게이트 선(ROI Line)을 긋고 이를 통과하는 차량 대수만 정밀하게 누적 카운팅하는 고급 비전 엔지니어링 기능 추가.

**13. 스트레스 테스트 시연 중 DB 오염(Pollution) 방지 설계**
- **이슈:** 시연용으로 혼잡도를 강제 뻥튀기하는 '정체 시나리오 테스트' 기능 작동 중, 생성된 가짜(Fake) 데이터가 그대로 Supabase 클라우드 운영 DB에 실시간으로 적재되어 통계를 망가뜨리는 치명적 결함 발견.
- **해결:** DB 비동기 적재 함수(`log_to_supabase_async`) 호출 직전에 `stress_test_active` 세션 상태를 검사하도록 `if` 예외 처리문을 추가. 테스트가 도는 15프레임(약 15초) 동안은 DB 기록을 일시 정지(Pause)시키고, 시연이 끝나면 자동으로 실제 CCTV 데이터 적재를 재개하도록 안전한 아키텍처로 개선 완료.

---
# AI_1team 프로젝트 진행 상황 (Phase 1 & Phase 2)

## 🎯 프로젝트 전체 목표
- **[Phase 1] Deep Learning 관제 (완료):** CCTV 기반 교통량/교통 상황 예측 및 분석 (YOLOv8 + GRU + LSTM Autoencoder)
- **[Phase 2] LLM 챗봇 연동 (진행 예정):** 실시간 교통 데이터를 분석하고 자연어로 사용자에게 상황을 보고/예측해 주는 지능형 AI 에이전트 구축

---

## 🏆 [Phase 1: Deep Learning] 딥러닝 비전 및 예측 파이프라인 최종 완성 내역 (Finalized)

본 섹션은 심사위원 방어 및 기술 시연(Demo)을 위해 최종적으로 확정된 핵심 기술 스펙과 트러블슈팅 내역을 요약합니다.

### 1. 웹 관제 대시보드 (Streamlit) 구축 완료
- **초고도화 UI/UX:** `app.py` 단일 파일로 실행되는 통합 웹 대시보드 구축.
- **핵심 3-Tab 체제 확립:** 
  1. `실시간 관제 (Live)`: CCTV 스트리밍 및 비전 분석.
  2. `모델 방어 보고서`: GRU 챔피언 모델 선정 논리(R² Score 0.871) 등 심사 방어 방패 마련.
  3. `아키텍처 & MLOps 파이프라인`: 시스템 다이어그램 및 Supabase 실시간 적재 데이터 EDA 테이블 병합.
- **2-Depth CCTV 필터링:** 고속도로(EX)와 국도/지방도(ITS) CCTV를 직관적으로 분리 선택할 수 있는 사이드바 UI 최적화.

### 2. 비전(Vision) 엔지니어링: 추적(Tracking) 및 필터링 고도화
- **독자적 센트로이드 트래커(Custom Centroid Tracker) 도입:** 
  - 기본 제공되는 ByteTrack이 프레임 스킵 환경에서 객체를 놓치는 치명적 버그(Frame Drop Issue)를 발견하고 전면 폐기.
  - 유클리디안 거리 연산 기반의 독자적인 2D 트래킹 알고리즘을 직접 코딩하여 인식률 개선.
- **정차 차량(Parked Car) 필터링:** 
  - 갓길 주차나 톨게이트 정차 차량 때문에 혼잡도가 폭증하는 오탐지(False Alarm)를 원천 차단.
  - 트래커가 10프레임 이상 1픽셀 미만으로 움직인 객체를 `[Parked]` 회색 박스로 분류하고, 혼잡도 연산에서 정상적으로 제외 처리.

### 3. 시계열 예측(Forecasting): GRU 딥러닝 앙상블 아키텍처
- **가짜 추세선(Linear Polyfit) 전면 폐기:** 점선이 튀는 것을 막기 위해 임시로 썼던 수학적 추세선을 삭제.
- **정통 Auto-Regressive GRU 복원:** 실제 훈련된 `forecaster_model.keras` (GRU) 모델을 복원하여 `t+1`부터 `t+5`까지 꼬리를 무는 다중 스텝 추론 구현.
- **[6:4 앙상블 기법] 극한의 안정성 확보:** 데이터 노이즈로 인한 예측 차트 널뛰기(Flickering)를 잡기 위해, `[딥러닝(GRU) 예측값 60%] + [과거 12프레임 시계열 트렌드 40%]`를 섞어내는 하이브리드 수식(Ensemble) 발명 및 적용 완료.

### 4. 돌발 정체 주입 (Stress Test) 시뮬레이션
- 심사위원 시연 시, 교통량이 한산한 도로에서도 강제로 차량 대수를 35대 추가하고 밀집도를 폭증시키는 `[🚨 돌발 정체 주입]` 토글 버튼 완성.
- 이를 통해 예측 모델(GRU)과 이상 탐지 모델(LSTM Autoencoder)이 어떻게 반응하고 경보(ANOMALY) 울리는지 명확한 시각적 검증 가능.

---

## 🚀 [Phase 2: LLM] 다음 세션에서 진행할 작업 (To-Do)

본격적으로 거대 언어 모델(LLM)을 프로젝트에 연동하여, 숫자로만 표시되던 관제 시스템에 '지능(Intelligence)'을 부여합니다.

## 🎯 4단계: 실시간 딥러닝 비전 + LLM 융합 (현재 진행 중)
본격적으로 거대 언어 모델(LLM)을 프로젝트에 연동하여, 숫자로만 표시되던 관제 시스템에 '지능(Intelligence)'을 부여합니다.

- `[x]` **LLM 기술 스택 선정:** Groq API(Llama 3)를 백엔드로 사용 결정 및 연동 완료.
- `[x]` **System Prompt (페르소나) 설계:** 
  - LLM에게 "ITS 관제 요원" 역할 부여 및 Streamlit 4번 탭에 채팅 UI 구축 완료.
- `[x]` **데이터 실시간 주입 (RAG/Context Injection):** 
  - 스트리밍 종료 직전의 실시간 데이터(live_metrics) 및 혼잡도 추이를 LLM에 주입 성공.
  - 대화 기록(Context Memory) 연동 로직 정상화 완료.
- `[x]` **대국민/관리자용 리포팅 자동화:** 
  - "현재 수원 신갈 IC 교통 상황 요약해 줘", "5분 뒤 정체될까?" 같은 질문에 LLM이 대답하는 로직 퀄리티 고도화 완료.
  - **[신규 킬러 기능]** 지정된 시간(1시간 등)의 DB 로그를 분석해 결재용 '관제 일지'를 자동 작성하는 RPA 기능(Auto Report Generator) 구현 완료.
- `[x]` **심사위원 어필 포인트 기획:** 단순한 챗봇이 아닌, 실시간 딥러닝 비전 데이터와 LLM이 상호작용하고 날씨나 과거 DB까지 융합하는 **"Vision-to-Text MLOps"** 구조임을 강조할 논리 및 실제 RAG 기능 구현 완료.

---

## 🛠️ 최근 트러블슈팅 및 아키텍처 결정 이력 (History)

**1. ITS 공공데이터 API 연결 복구**
- 기존 `urllib` 기반 코드에서 `HTTPSConnectionPool` 에러 발생.
- `requests` 라이브러리로 전면 교체 후 JSON 파싱 로직 최적화하여 정상 작동 확인.

**2. LLM 대화 기억 상실(Amnesia) 버그 해결**
- **이슈:** 챗봇이 직전에 나눈 대화를 기억하지 못하고 매번 초기화됨.
- **원인:** Groq API에 새 질문 1개만 단독으로 전달하고 있었음.
- **해결:** `st.session_state.messages` 전체를 API에 전달. 단, **Llama-3 모델은 대화가 반드시 User 역할로 시작해야 하는 제약(400 Bad Request)**이 있어, 챗봇의 첫 초기 환영 인사(assistant)는 Context에서 제외하는 예외 처리 로직 추가 완료.

**3. Streamlit 동시성(UI 블로킹) 한계 극복 시도 및 롤백**
- **목표:** CCTV 영상(무한 루프)이 돌아가는 중에도 사용자가 4번 탭에서 LLM 채팅을 칠 수 있도록 만들기 위해 시도.
- **과정:** 영상 처리를 별도의 백그라운드 스레드(`video_worker`)로 빼고, 프론트엔드는 `st_autorefresh`로 0.5초마다 갱신하는 멀티스레딩 아키텍처 도입.
- **이슈:** 
  - 파이썬 GIL(Global Interpreter Lock) 제약과 데모 영상(MP4)의 빠른 재생 속도가 충돌하여 CPU 100% 점유 현상 및 전체 화면 프리징(멈춤) 발생.
  - 차트(Vega-Lite)를 초당 여러 번 억지로 렌더링하면서 극심한 프론트엔드 렉 발생 및 `FutureWarning` 경고 발생.
- **결정 (Rollback):** 무리한 멀티스레딩이 오히려 시연 안정성을 크게 해치므로, **가장 안정적인 단일 스레드(Single-Thread) 원본 코드로 전면 롤백 결정.** (`frame_skip = 10` 원복)
- **최종 시연 시나리오 확정:** 영상 스트리밍을 켜서 분석 결과를 충분히 확보한 뒤, **스트리밍 체크박스를 끄고 4번 탭으로 넘어가서 분석된 최신 데이터(`live_metrics`)를 바탕으로 LLM과 대화하는 안정적인 시퀀스**로 채택.

**4. LLM 완전한 '문맥' 및 '데이터' 기억 상실 버그 해결**
- **이슈:** 챗봇이 이전 대화 내용을 하나도 기억 못하고, 실시간 CCTV 데이터조차 '알 수 없음'으로 대답함.
- **해결 1 (문맥 상실):** 사용자의 질문만 `st.session_state.messages`에 저장하고 AI의 답변은 저장하지 않는 치명적 버그 발견. AI 답변 직후 기록을 append 하도록 로직 수정 및 중복 append 버그 동시 해결.
- **해결 2 (데이터 상실):** 이전 원본 코드로 롤백하는 과정에서 스트리밍 루프(`while run_stream:`) 내부에 있던 `live_metrics` 업데이트 코드가 날아간 것을 확인하여 다시 복구.

**5. Supabase 연동 실시간 과거 데이터 RAG 파이프라인 구축**
- **목표:** "최근 1분간의 평균 통행량은 어때?" 와 같은 과거 데이터 질의에 챗봇이 정확히 대답하도록 개선.
- **해결:** LLM 호출 직전에 Supabase DB에서 최근 60개(1분 분량)의 로그를 긁어와 승용차, 버스, 트럭의 평균 통행량과 혼잡도를 계산한 뒤 LLM의 System Prompt에 `[과거 누적 통계 데이터]`라는 명찰을 달아 강제 주입함. 프롬프트 수칙을 엄격하게 분리하여 '현재'와 '과거'를 혼동하지 않도록 튜닝 완료.

**6. 채팅창 UI 프레임 분리 (UX 개선)**
- **이슈:** 대화가 길어질수록 웹페이지가 무한정 아래로 길어지는 촌스러운 UI.
- **해결:** Streamlit 1.35+ 의 `st.container(height=600)` 기능을 사용하여 카카오톡이나 ChatGPT처럼 내부에 스크롤이 생기는 독립된 고정 크기 채팅창(프레임)으로 UI 퀄리티 대폭 업그레이드.

**7. 킬러 기능 추가: 관제 일지 원클릭 자동 생성기 (RPA)**
- **목표:** 단순한 챗봇 응답을 넘어선 실무 수준의 관리자용 리포팅 업무 자동화 구현.
- **해결:** 채팅창 상단에 전용 Expander를 만들어, '최근 10분, 30분, 1시간'의 방대한 DB 데이터를 한 번에 가져와 LLM에게 전문적인 마크다운 형식의 결재용 '지능형 ITS 교통 관제 일지'를 작성하도록 지시하는 버튼과 로직 구현 완료. (심사위원 어필용 핵심 포인트 확보)

**8. LLM 자율 DB 조회 (Function Calling) 도입 및 UI 분리**
- **목표:** 기존에 고정된 1분(60개) 분량의 과거 데이터만 주입하던 한계를 극복하고, 사용자의 질문에 따라 필요한 시간대의 데이터를 유연하게 조회.
- **해결:** Llama-3 모델의 `tool_calls` 기능을 활용하여 `get_traffic_data_from_db` 함수를 도구로 제공. LLM이 스스로 과거 조회가 필요하다고 판단하면 해당 도구를 호출하여 지정된 분(minutes) 만큼 Supabase에서 가져와 대답하도록 개선(Agentic Workflow 적용).
- **UI 개선:** 기존 4번 탭에 있던 '관제 일지 자동 생성기' 기능을 별도의 **5번 탭**으로 독립적으로 분리하여 챗봇 UI의 쾌적함 확보.

**9. LLM DB 연동(Function Calling) 치명적 버그 해결 및 정상화**
- **이슈:** Llama 3.1 8B, Mixtral 모델 등에서 DB 조회 시 `tool_use_failed` 또는 `model_decommissioned` 환각/에러가 지속적으로 발생하여 정확한 과거 조회가 불가능.
- **해결:** 에러를 뱉는 폐기된 모델 및 불안정한 경량 모델들을 UI 옵션에서 모두 제거. 모델이 시간/날짜 범위를 올바르게 인식하도록 백엔드에 정교한 정규식 기반 시간 파서(Date Parser)를 구축. 충분한 토큰이 확보된 `llama-3.3-70b-versatile` 모델을 기본 탑재하여, "어제 교통량", "최근 1시간", "7월 1일" 등의 쿼리를 정밀한 크로스체크(실제 DB 수치와 정확히 일치) 수준으로 응답하도록 환각(Hallucination) 버그 제어.
- **추가:** 클라우드 LLM 토큰 고갈 및 장애 상황을 대비하여, Local LLM(Ollama) 베이스 URL 인젝션 기능을 추가하여 오프라인/무제한 환경에서도 시스템이 구동될 수 있는 안전장치 마련.

**10. 관제 일지(보고서) Word 파일(.docx) 자동 추출 기능 신설**
- **목표:** 기존 5번 탭의 단순 텍스트 출력 방식에서 한 단계 나아가, 실무진들이 2차 가공 및 실제 결재에 즉각 활용할 수 있는 문서 포맷 제공.
- **해결:** `python-docx` 라이브러리를 도입. LLM이 생성한 마크다운 문법(Heading, Bullet points 등)을 파이썬 백엔드에서 실시간으로 인메모리 파싱하여 굵은 글씨, 폰트 사이즈가 적용된 정식 회사 결재 양식 형태의 `.docx` 파일로 조립한 후 사용자에게 다운로드 버튼 제공.

**11. UI/UX 전면 프리미엄화 (다크 모드 & 글래스모피즘)**
- **목표:** Streamlit의 기본 화이트보드 템플릿을 탈피하여, 공상과학 수준의 미래지향적/프리미엄 관제 대시보드 시각화.
- **해결:** `st.markdown` Custom CSS 인젝션을 활용. 글로벌 폰트를 가독성이 뛰어난 'Pretendard'로 강제 적용하고, 메인 배경을 네이비 블루 그라데이션으로 전면 교체. 지표(Metric) 카드와 탭 컴포넌트에 반투명 유리 질감(Glassmorphism) 및 마우스 오버 시 애니메이션(Hover Transition)을 주입하여 세련된 상용 어플리케이션 퀄리티 확보.

**12. 딥러닝 비주얼 고도화 (실시간 혼잡도 히트맵 및 가상 통제선 카운팅)**
- **목표:** 백엔드에서만 돌아가는 AI 모델의 위력을 심사위원들이 화면상에서 직관적으로 체감할 수 있도록 시각적 임팩트(Visual Impact) 부여.
- **해결:** OpenCV의 가우시안 필터링과 Jet 컬러맵을 조합하여 차량 밀집 구간을 붉게 물들이는 '실시간 히트맵(Heatmap)' 알고리즘을 구축. 더불어, 객체의 고유 ID를 기억하는 트래킹 기술을 응용하여 화면 특정 위치(y=0.6)에 가상의 톨게이트 선(ROI Line)을 긋고 이를 통과하는 차량 대수만 정밀하게 누적 카운팅하는 고급 비전 엔지니어링 기능 추가.

**13. 스트레스 테스트 시연 중 DB 오염(Pollution) 방지 설계**
- **이슈:** 시연용으로 혼잡도를 강제 뻥튀기하는 '정체 시나리오 테스트' 기능 작동 중, 생성된 가짜(Fake) 데이터가 그대로 Supabase 클라우드 운영 DB에 실시간으로 적재되어 통계를 망가뜨리는 치명적 결함 발견.
- **해결:** DB 비동기 적재 함수(`log_to_supabase_async`) 호출 직전에 `stress_test_active` 세션 상태를 검사하도록 `if` 예외 처리문을 추가. 테스트가 도는 15프레임(약 15초) 동안은 DB 기록을 일시 정지(Pause)시키고, 시연이 끝나면 자동으로 실제 CCTV 데이터 적재를 재개하도록 안전한 아키텍처로 개선 완료.

---

## 🎯 5단계: 최종 산출물 및 프레젠테이션(PPT) 방어 논리 구축 (완료)
최종 심사위원 평가를 대비하여, 딥러닝과 LLM 엔지니어링의 성과를 극대화할 수 있는 프레젠테이션 대본 및 에셋을 생성 완료했습니다.

- `[x]` **공식 산출물 구조 동기화:** 제공된 예시 PDF의 7대 공식 목차 구조를 엄격히 준수하는 최종 프레젠테이션 대본(`최종_프레젠테이션_대본.md`) 작성 완료.
- `[x]` **엔지니어링 근거(Evidence) 장표 보강:** 데이터 전처리(Log-Transformation), 하이퍼파라미터 튜닝(LSTM 16-Neuron), 챔피언 모델 선정 사유(GRU) 등 치열한 기술적 근거를 논리적으로 방어하는 대본 구성.
- `[x]` **고급 LLM 역량 어필:** 단순 챗봇 수준을 넘어선 Function Calling(자율형 에이전트), RAG, RPA(.docx 자동 추출) 기술을 핵심 킬러 기능으로 격상시켜 대본에 반영.
- `[x]` **XAI(설명가능 AI) 팩트 체크 및 논리 강화:** 딥러닝(GRU) 모델의 결과 해석을 위해 단순 Feature Importance가 아닌 정통 **'SHAP'** 라이브러리를 직접 연동하여 분석했음을 최종 검증하고 프레젠테이션의 핵심 무기로 확정.
- `[x]` **실무 트러블 슈팅 스토리텔링:** 클라우드 DB 오염 방지 이슈 및 LLM 환각 제어 이슈를 트러블 슈팅 사례로 배치하여 실무적 역량 어필.
- `[x]` **AI 디자인 툴 프롬프트 최적화:** 미리캔버스, Gamma 등의 AI 생성기가 수준 높은 B2B 제안서 스타일을 출력할 수 있도록 맞춤형 디자인 프롬프트(`PPT_디자인_프롬프트.md`, `PPT_디자인_프롬프트_요약본.md`) 에셋 구축.
- `[x]` **최종 발표 자료(PPT) 목차 및 내용 정합성 검증:** Python 스크립트(`python-pptx`)를 통해 작성된 PPT의 텍스트를 정밀 추출하여, 슬라이드 2번의 전체 목차와 실제 본문 슬라이드들의 헤드라인/소제목 워딩을 1:1로 완벽히 일치시키도록 검수 및 피드백 완료.
- `[x]` **아키텍처 시각화(Mermaid) 안정성 확보:** PPT 삽입용 아키텍처 다이어그램(`llm_flowcharts.md`)에서 발생한 마크다운 렌더링 충돌 오류를 해결하기 위해, 이모지와 특수 태그를 모두 배제한 가장 안정적인 표준 문법으로 순서도 재설계 완료.
- `[x]` **실전 프레젠테이션(무대 발표) 전략 수립:** "설명 글씨 0% + 아키텍처 순서도 50% + 터미널 증거 스크린샷 50%"라는 레이아웃 황금 비율을 확립하고, 모니터를 볼 수 없는 무대 발표 환경에 맞춘 '큐카드(종이 대본) 활용 및 다이어그램 지시형 발표 기법' 가이드라인 제공.
- `[x]` **최종 산출물 최적화 및 리소스 정리:** 참조가 끝난 외부 에셋(PDF 예시 폴더)과 혼선을 줄 수 있는 과거 실험용 중복 파일(`Total_Pipeline.ipynb`) 등을 모두 영구 삭제하여 프로젝트 폴더의 형상을 최종 운영 수준으로 가장 깔끔하게 정돈 완료.
- `[x]` **최종 형상 관리 (Git):** 생성된 모든 대본, 최신 주피터 노트북, 그리고 무거운 가중치 모델 파일들을 포함하여 Github 원격 레포지토리(`origin/main`)에 성공적으로 푸시 및 백업 완료.
